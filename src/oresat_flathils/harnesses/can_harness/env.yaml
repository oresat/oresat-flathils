# Labgrid environment configuration for a socketCAN network interface.
# The physical adapter is identified and normalized 
# by a udev rule on the host, this file has no adapter-specific detail.
targets:
  main:
    resources:
      NetworkInterface:
        ifname: 'flathilscan0'