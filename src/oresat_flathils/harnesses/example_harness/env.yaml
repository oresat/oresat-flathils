# A basic labgrid environment configuration. This example sets up a USB serial
# port for a Raspberry Pi Pico (RP2040).
targets:
  main:
    resources:
      USBSerialPort:
        match:
          ID_VENDOR_ID: "2e8a"
        speed: 115200
    drivers:
      SerialDriver: {}
