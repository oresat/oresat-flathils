targets:
  main:
    resources:
      USBSerialPort:
        match:
          ID_VENDOR_ID: "2e8a"       # Raspberry Pi Foundation (CircuitPython)
          ID_USB_INTERFACE_NUM: "00" # the console CDC, not the data CDC
        speed: 115200
    drivers:
      SerialDriver: {}
